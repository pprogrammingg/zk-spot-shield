# Day 2 
  - [ ] Implement `GlobalConfig` PDA (authority, vkey hash, pause flag)
  - [ ] Register it in the program module tree

## Task Overview

Build the core administration and security state for your on-chain settlement engine: a Program Derived Address (PDA) called **GlobalConfig**.

## What the Task Involves

You will create a singleton on-chain account stored at a deterministic address that holds three vital properties:

| Property     | Type       | Description                                                                                                    |
| ------------ | ---------- | -------------------------------------------------------------------------------------------------------------- |
| `authority`  | `Pubkey`   | The public key of the admin wallet authorized to update protocol parameters or invoke emergency controls       |
| `vkey_hash`  | `[u8; 32]` | The cryptographic fingerprint of your off-chain SP1 ZK circuit                                                 |
| `pause_flag` | `bool`     | A global boolean switch (`is_paused`) used to freeze settlement activity if a vulnerability or incident occurs |

## Why It Is Necessary

Without **GlobalConfig**, the on-chain engine has no operational boundary or verification anchor.

### 🔐 Authentication for ZK Proofs

* Off-chain ZK proofs (Groth16/SP1) prove that execution matched a specific circuit.
* The on-chain verifier CPI needs to compare the proof's verification key against a trusted key stored on-chain.
* `vkey_hash` acts as that source of truth, preventing malicious actors from submitting valid ZK proofs generated by a rogue or modified circuit.

### 🏛️ Protocol Governance

* Having a registered authority ensures that critical management functions—such as rotating the circuit key after an off-chain upgrade or pausing the protocol—can only be triggered by the authorized system owner.

### 🚨 Emergency Circuit Breaker

* If a bug, oracle exploit, or front-running vector is discovered off-chain, the `is_paused` flag allows the authority to immediately freeze settlements, protecting user reserves in the vault.

### 📍 Single Source of Truth

* Deriving this account as a canonical PDA ensures that all settlement instructions across the entire program read from one immutable, tamper-proof configuration account.

# Day 3

ChatGPT said:
## Key Differences: `#[account]` vs `#[account(zero_copy)]`

| Feature | Standard `#[account]` | `#[account(zero_copy)]` |
|---|---|---|
| **On-Chain Account?** | Yes | Yes |
| **8-Byte Discriminator** | Placed automatically at the start of account data | Placed automatically at the start of account data |
| **Deserialization** | Copies account bytes into a new Rust struct instance on every instruction call | Zero-copy: maps the account data buffer directly into the struct |
| **Account Context Type** | `Account<'info, VaultState>` | `AccountLoader<'info, VaultState>` |
| **Max Account Size** | Subject to stack/heap allocation limits (~10 KB) | Supports large accounts, up to ~10 MB |
| **Typical Use Case** | Normal-sized account state | Large account